<%= form_with(model: @event, local: true) do |f| %>
    <div class="mb-3">
        <%= f.label :title, class: 'form-label' %>
        <%= f.text_field :title, class: 'form-control' %>
    </div>

    <div class="mb-3">
        <%= f.label :category, class: 'form-label' %>
        <%= f.select :category, options_for_select([['Tech', 'tech'], ['Music', 'music'], ['Sports', 'sports']], @event.category), {}, class: 'form-select' %>
    </div>

    <div class="mb-3">
        <%= f.label :venue, class: 'form-label' %>
        <%= f.select :venue, options_for_select([['Auditorium', 'auditorium'], ['Main Hall', 'main_hall'], ['Campus Field', 'campus_field']], @event.venue), {}, class: 'form-select' %>
    </div>

    <div class="mb-3">
        <%= f.label :start_date, class: 'form-label' %>
        <%= f.datetime_local_field :start_date, class: 'form-control' %>
    </div>

    <div class="mb-3">
        <%= f.label :end_date, class: 'form-label' %>
        <%= f.datetime_local_field :end_date, class: 'form-control' %>
    </div>

    <div class="mb-3">
        <%= f.label :maximum_capacity, class: 'form-label' %>
        <%= f.number_field :maximum_capacity, class: 'form-control' %>
    </div>

    <div class="mb-3">
        <%= f.label :description, class: 'form-label' %>
        <%= f.text_area :description, class: 'form-control', rows: 5 %>
    </div>

    <div class="mb-3">
        <%= f.submit class: 'btn btn-primary' %>
    </div>
<% end %>